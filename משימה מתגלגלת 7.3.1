def is_valid_input(letter):
    if len(letter) == 1:
        check_len = True
    else:
        check_len = False

    if letter.isupper() or letter.islower():
        check_letter = True
    else:
        check_letter = False

    if (check_len == True) and (check_letter == True):
        result = True
    else:
        result = False
    return result

def try_update_letter_guess(letter_guessed, old_letter):
    if old_letter.__contains__(letter_guessed) :
        print 'X guessed before'
    else:
        letter_guessed = str(letter_guessed.lower())
        old_letter += letter_guessed
        print guess.lower()
        print "_" * length

def show_hidden_word(secret_word, old_letter_guess):
    result = []
    for i in secret_word:
        if i in old_letter_guess:
            result.append(i)
        else:
            result.append("_")
    result = str(result).replace("'", " ")
    result = str(result).replace(",", " ")
    return result

HANGMAN_ASCII_ART = """
  _    _
 | |  | |
 | |__| | __ _ _ __   __ _ _ __ ___   __ _ _ __
 |  __  |/ _` | '_ \ / _` | '_ ` _ \ / _` | '_ \\
 | |  | | (_| | | | | (_| | | | | | | (_| | | | |
 |_|  |_|\__,_|_| |_|\__, |_| |_| |_|\__,_|_| |_|
                      __/ |
                     |___/ """

MAX_TRIES = 6

print HANGMAN_ASCII_ART
print ".............................................................................."
print ".............................................................................."
print ".............................................................................."
print ".............................................................................."

print 'you have', MAX_TRIES, 'tries'
#start screen
guess = input('put letter')
length = len(guess)
old_letter = ['a', 'b', 'c']
check = is_valid_input(guess)
secret_word = 'mammals'

if check == True:
    print try_update_letter_guess(guess, old_letter)
print show_hidden_word(secret_word, old_letter)